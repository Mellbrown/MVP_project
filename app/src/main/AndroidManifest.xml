<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.techwork.kjc.mvp_project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".controller.TestController">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.ACT1_Splash" />
        <activity android:name=".activity.ACT2_Register" />
        <activity android:name=".activity.ACT3_Login" />
        <activity android:name=".activity.ACT5_Measure" />
        <activity android:name=".util.PhotoProcess" />
    </application>

</manifest>